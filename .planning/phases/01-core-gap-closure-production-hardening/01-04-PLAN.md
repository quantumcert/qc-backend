---
phase: 01-core-gap-closure-production-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - prisma/schema.prisma
  - src/services/core-facets/CurationFacet.ts
  - src/routes/v1/publicRoutes.ts
  - src/routes/v1/contributionRoutes.ts
  - src/routes/index.ts
  - src/controllers/ContributionController.ts
  - tests/curation-facet.test.ts
  - tests/curation-routes.test.ts
autonomous: true
requirements:
  - CORE-05
  - CORE-06

must_haves:
  truths:
    - "POST /api/v1/public/asset/:assetId/contribution de não-auditor cria PendingContribution com status PENDING_APPROVAL"
    - "POST /api/v1/public/asset/:assetId/contribution de auditor (registrado em Contributor com isAuditor=true) cria EventLog APPROVED diretamente"
    - "POST /api/v1/contributions/:id/review com role OPERATOR aprova → cria EventLog + dispara AnchorQueue"
    - "POST /api/v1/contributions/:id/review com role OPERATOR rejeita → atualiza status REJECTED com reason"
    - "POST /api/v1/contributions/:id/review com role READER retorna 403"
    - "Reviewer só pode aprovar/rejeitar contribuições do próprio tenantId (tenant isolation)"
    - "Schema Prisma push aplicado em DB com novos modelos Contributor + PendingContribution + enum PendingContributionStatus"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Models Contributor, PendingContribution, enum PendingContributionStatus"
      contains: "model Contributor"
    - path: "src/services/core-facets/CurationFacet.ts"
      provides: "submitContribution + reviewContribution"
      contains: "PENDING_APPROVAL"
    - path: "src/routes/v1/publicRoutes.ts"
      provides: "POST /asset/:assetId/contribution rota pública"
      contains: "/asset/:assetId/contribution"
    - path: "src/routes/v1/contributionRoutes.ts"
      provides: "POST /:id/review rota autenticada"
      contains: "/:id/review"
  key_links:
    - from: "src/services/core-facets/CurationFacet.ts"
      to: "src/services/AnchorQueueService.ts"
      via: "AnchorQueueService.processQueue() fire-and-forget"
      pattern: "AnchorQueueService\\.processQueue"
    - from: "src/routes/v1/publicRoutes.ts"
      to: "src/services/core-facets/CurationFacet.ts"
      via: "router.post handler chama submitContribution"
      pattern: "CurationFacet\\.submitContribution"
    - from: "src/routes/v1/contributionRoutes.ts"
      to: "src/services/core-facets/CurationFacet.ts"
      via: "router.post handler chama reviewContribution"
      pattern: "CurationFacet\\.reviewContribution"
---

<objective>
Implementar a Curation Layer (CORE-05 + CORE-06): contribuições públicas de não-auditores
entram em fila `PENDING_REVIEW` e são revisadas por auditores (OPERATOR/ADMIN) com
registro em EventLog.

Purpose: Hoje qualquer pessoa pode submeter contribuições que vão direto para EventLog
(sem controle de qualidade). A camada de curadoria filtra contribuições por
"contributor confiável" e força revisão manual para os demais.

Output: 2 novos modelos Prisma, 1 novo Facet (`CurationFacet`), 2 novas rotas
(pública para submit, autenticada para review), schema sincronizado com DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-gap-closure-production-hardening/01-RESEARCH.md
@.planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md
@CLAUDE.md
@docs/superpowers/specs/2026-05-08-curation-layer-design.md
@prisma/schema.prisma
@src/services/core-facets/EventLogFacet.ts
@src/services/core-facets/BlindContactLogFacet.ts
@src/services/core-facets/TransferRegistryFacet.ts
@src/services/core-facets/LifecycleFacet.ts
@src/routes/v1/publicRoutes.ts
@src/routes/index.ts

<interfaces>
<!-- Contratos para os artefatos a serem criados -->

NEW: src/services/core-facets/CurationFacet.ts
```typescript
export class CurationFacet {
  /** Public submission — phone or email required, no API key */
  static async submitContribution(params: {
    assetId: string;
    phone?: string;
    email?: string;
    payload: Record<string, any>;
  }): Promise<{ queued: boolean; eventId?: string; pendingId?: string }>;

  /** Authenticated review — OPERATOR or ADMIN only */
  static async reviewContribution(
    secureContext: { tenantId: string; role: string; apiKeyId: string },
    payload: { pendingId: string; decision: 'APPROVED' | 'REJECTED'; reason?: string }
  ): Promise<{ pendingId: string; status: 'APPROVED' | 'REJECTED'; eventId?: string }>;
}
```

NEW: prisma/schema.prisma (additions)
```prisma
model Contributor {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  ownerRef  String   // phone, email, ou identifier
  isAuditor Boolean  @default(false)
  createdAt DateTime @default(now())
  @@unique([tenantId, ownerRef])
  @@index([tenantId])
}

model PendingContribution {
  id         String                    @id @default(cuid())
  tenantId   String
  tenant     Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  ownerId    String
  assetId    String?
  payload    Json
  status     PendingContributionStatus @default(PENDING_APPROVAL)
  reviewedBy String?
  reviewedAt DateTime?
  createdAt  DateTime                  @default(now())
  @@index([tenantId, status])
  @@index([createdAt])
}

enum PendingContributionStatus {
  PENDING_APPROVAL
  APPROVED
  REJECTED
}

// model Tenant adições:
//   contributors         Contributor[]
//   pendingContributions PendingContribution[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar models Contributor + PendingContribution ao schema Prisma</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (TODO o arquivo — confirmar onde estão modelos existentes, onde estão os enums; verificar exatamente quais relações o model Tenant já declara)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção schema.prisma — exemplo de EventLog como template)
    - docs/superpowers/specs/2026-05-08-curation-layer-design.md (spec aprovada, fonte de verdade para campos)
  </read_first>
  <action>
    1. Adicionar os models `Contributor` e `PendingContribution` ao schema, depois do bloco de modelos existentes e antes da seção de enums (ou em qualquer posição respeitando o agrupamento atual). Usar EXATAMENTE o formato da seção <interfaces> deste plano.
    2. Adicionar o enum `PendingContributionStatus` junto com os outros enums no fim do arquivo (procurar `enum PlanTier` ou `enum ApiKeyRole` para localizar a seção).
    3. Adicionar as duas relações ao model `Tenant`:
       ```prisma
       contributors         Contributor[]
       pendingContributions PendingContribution[]
       ```
       Inserir no bloco de relações do Tenant (em paralelo com as outras coleções tipo `assets Asset[]`, `eventLogs EventLog[]` etc.).
    4. NÃO modificar nenhum outro modelo. NÃO renomear campos existentes.
    5. NÃO rodar prisma db push aqui — isso é feito na Task 4 (BLOCKING).
  </action>
  <verify>
    <automated>npx prisma format 2>&1 | tail -5 && npx prisma validate 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^model Contributor " prisma/schema.prisma` retorna 1
    - `grep -c "^model PendingContribution " prisma/schema.prisma` retorna 1
    - `grep -c "^enum PendingContributionStatus " prisma/schema.prisma` retorna 1
    - `grep -c "PENDING_APPROVAL" prisma/schema.prisma` retorna >= 1
    - `grep -c "contributors.*Contributor\\[\\]" prisma/schema.prisma` retorna 1 (relação no Tenant)
    - `grep -c "pendingContributions.*PendingContribution\\[\\]" prisma/schema.prisma` retorna 1
    - `npx prisma validate` retorna "The schema at prisma/schema.prisma is valid"
  </acceptance_criteria>
  <done>
    Schema Prisma é válido e contém os novos modelos. Client ainda não regenerado (Task 4 fará).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar CurationFacet — submitContribution + reviewContribution</name>
  <files>src/services/core-facets/CurationFacet.ts, tests/curation-facet.test.ts</files>
  <read_first>
    - src/services/core-facets/EventLogFacet.ts (linhas 56-84 — padrão de transação com EventLog + AnchorQueue fire-and-forget; linhas 98-105 — lookup asset → tenantId)
    - src/services/core-facets/BlindContactLogFacet.ts (linhas 1-50 — padrão de Facet público)
    - src/services/core-facets/TransferRegistryFacet.ts (linhas 25-38 — padrão de RBAC inline e tenant isolation)
    - src/services/core-facets/LifecycleFacet.ts (linhas 22-26 — função makeError helper)
    - src/services/AnchorQueueService.ts (apenas para confirmar import path)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção CurationFacet — skeleton + analogs)
    - docs/superpowers/specs/2026-05-08-curation-layer-design.md (5 casos de teste definidos na spec)
  </read_first>
  <behavior>
    - Test 1 (submit/auditor): Contribuidor com Contributor.isAuditor=true para o tenant do asset → cria EventLog status APPROVED + retorna { queued: true, eventId }
    - Test 2 (submit/non-auditor): Contribuidor sem registro ou com isAuditor=false → cria PendingContribution status PENDING_APPROVAL + retorna { queued: false, pendingId }
    - Test 3 (submit/asset não existe): assetId inválido → throw com code ASSET_NOT_FOUND
    - Test 4 (submit/payload inválido): nem phone nem email presentes → throw com code INVALID_PAYLOAD
    - Test 5 (review/RBAC): role READER → throw INSUFFICIENT_PERMISSIONS
    - Test 6 (review/cross-tenant): pendingId pertence a outro tenant → throw CONTRIBUTION_NOT_FOUND (tenant isolation)
    - Test 7 (review/APPROVED): OPERATOR aprova → PendingContribution.status = APPROVED + cria EventLog APPROVED + chama AnchorQueueService.processQueue (fire-and-forget; mockar)
    - Test 8 (review/REJECTED): OPERATOR rejeita com reason → PendingContribution.status = REJECTED, reviewedBy = apiKeyId, reviewedAt set
  </behavior>
  <action>
    1. Criar `src/services/core-facets/CurationFacet.ts` com a estrutura skeleton de PATTERNS.md, implementando os métodos:

    ```typescript
    import prisma from '../../config/prisma';
    import { AnchorQueueService } from '../AnchorQueueService';
    import crypto from 'crypto';

    function makeError(message: string, code: string, httpStatus: number): Error {
      const err: any = new Error(message);
      err.code = code;
      err.httpStatus = httpStatus;
      return err;
    }

    export class CurationFacet {
      static async submitContribution(params: {
        assetId: string;
        phone?: string;
        email?: string;
        payload: Record<string, any>;
      }): Promise<{ queued: boolean; eventId?: string; pendingId?: string }> {
        const { assetId, phone, email, payload } = params;
        if (!phone && !email) {
          throw makeError('phone or email required', 'INVALID_PAYLOAD', 400);
        }
        const ownerRef = phone ?? email!;

        const asset = await prisma.asset.findUnique({ where: { id: assetId } });
        if (!asset) throw makeError('Asset not found', 'ASSET_NOT_FOUND', 404);

        const contributor = await prisma.contributor.findUnique({
          where: { tenantId_ownerRef: { tenantId: asset.tenantId, ownerRef } },
        });
        const isAuditor = contributor?.isAuditor === true;

        if (isAuditor) {
          const signatureHash = crypto.createHash('sha3-512')
            .update(JSON.stringify(payload)).digest('hex');
          const event = await prisma.eventLog.create({
            data: {
              assetId,
              tenantId: asset.tenantId,
              origin: `CURATION:${ownerRef}`,
              status: 'APPROVED',
              payload,
              signatureHash,
            },
          });
          // TODO(OPS-03): substituir console.error por logger estruturado (Phase 4)
          AnchorQueueService.processQueue().catch(console.error);
          return { queued: true, eventId: event.id };
        }

        const pending = await prisma.pendingContribution.create({
          data: {
            tenantId: asset.tenantId,
            ownerId: ownerRef,
            assetId,
            payload,
            status: 'PENDING_APPROVAL',
          },
        });
        return { queued: false, pendingId: pending.id };
      }

      static async reviewContribution(
        secureContext: { tenantId: string; role: string; apiKeyId: string },
        payload: { pendingId: string; decision: 'APPROVED' | 'REJECTED'; reason?: string }
      ): Promise<{ pendingId: string; status: 'APPROVED' | 'REJECTED'; eventId?: string }> {
        const { tenantId, role, apiKeyId } = secureContext;
        const { pendingId, decision, reason } = payload;

        if (role !== 'ADMIN' && role !== 'OPERATOR') {
          throw makeError('Forbidden', 'INSUFFICIENT_PERMISSIONS', 403);
        }
        if (decision !== 'APPROVED' && decision !== 'REJECTED') {
          throw makeError('Invalid decision', 'INVALID_PAYLOAD', 400);
        }

        const pending = await prisma.pendingContribution.findFirst({
          where: { id: pendingId, tenantId },
        });
        if (!pending) {
          throw makeError('Contribution not found', 'CONTRIBUTION_NOT_FOUND', 404);
        }
        if (pending.status !== 'PENDING_APPROVAL') {
          throw makeError('Contribution already reviewed', 'ALREADY_REVIEWED', 409);
        }

        if (decision === 'APPROVED') {
          const result = await prisma.$transaction(async (tx) => {
            await tx.pendingContribution.update({
              where: { id: pendingId },
              data: { status: 'APPROVED', reviewedBy: apiKeyId, reviewedAt: new Date() },
            });
            const signatureHash = crypto.createHash('sha3-512')
              .update(JSON.stringify(pending.payload)).digest('hex');
            const event = await tx.eventLog.create({
              data: {
                assetId: pending.assetId!,
                tenantId,
                origin: `CURATION_REVIEW:${apiKeyId}`,
                status: 'APPROVED',
                payload: pending.payload as any,
                signatureHash,
              },
            });
            return { eventId: event.id };
          });
          // TODO(OPS-03): substituir console.error por logger estruturado (Phase 4)
          AnchorQueueService.processQueue().catch(console.error);
          return { pendingId, status: 'APPROVED', eventId: result.eventId };
        } else {
          await prisma.pendingContribution.update({
            where: { id: pendingId },
            data: {
              status: 'REJECTED',
              reviewedBy: apiKeyId,
              reviewedAt: new Date(),
              payload: { ...(pending.payload as any), _rejectionReason: reason ?? null },
            },
          });
          return { pendingId, status: 'REJECTED' };
        }
      }
    }
    ```

    2. Criar `tests/curation-facet.test.ts` com os 8 casos do behavior. Setup: criar tenant + asset de teste; criar Contributor com isAuditor=true para um caso e false/ausente para outro. Mockar AnchorQueueService.processQueue para evitar trigger real.
    3. NÃO adicionar selector ao FacetRegistry (decisão arquitetural — rota pública direta, não via Diamond, conforme spec).
  </action>
  <verify>
    <automated>npm test -- tests/curation-facet.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/services/core-facets/CurationFacet.ts` existe e exporta classe `CurationFacet`
    - `grep -c "submitContribution" src/services/core-facets/CurationFacet.ts` retorna >= 2 (declaração + uso interno se houver)
    - `grep -c "reviewContribution" src/services/core-facets/CurationFacet.ts` retorna >= 1
    - `grep -c "PENDING_APPROVAL" src/services/core-facets/CurationFacet.ts` retorna >= 1
    - `grep -c "AnchorQueueService.processQueue" src/services/core-facets/CurationFacet.ts` retorna >= 2 (auditor branch + approval branch)
    - `grep -c "ASSET_NOT_FOUND\\|INVALID_PAYLOAD\\|INSUFFICIENT_PERMISSIONS\\|CONTRIBUTION_NOT_FOUND" src/services/core-facets/CurationFacet.ts` retorna >= 4 (todos os codes acima)
    - `grep -c "FacetRegistry\\|facetRegistry" src/diamond/FacetRegistry.ts | grep -c "contribution"` é 0 (NÃO foi adicionado ao registry — confirmar apenas inspecionando)
    - Arquivo `tests/curation-facet.test.ts` existe com >= 8 testes
    - `npm test -- tests/curation-facet.test.ts` passa
  </acceptance_criteria>
  <done>
    CurationFacet implementa os dois métodos com tenant isolation, RBAC, fire-and-forget AnchorQueue.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adicionar rotas pública e autenticada de curadoria</name>
  <files>src/routes/v1/publicRoutes.ts, src/routes/v1/contributionRoutes.ts, src/controllers/ContributionController.ts, src/routes/index.ts, tests/curation-routes.test.ts</files>
  <read_first>
    - src/routes/v1/publicRoutes.ts (linhas 72-85 — padrão de rota pública)
    - src/routes/index.ts (linhas 42-49 — padrão de mount)
    - src/controllers/BlindContactController.ts (linhas 1-41 — template slim para ContributionController)
    - src/middleware/apiKeyAuth.ts e src/middleware/rbacGuard.ts (entender requireOperator)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seções publicRoutes.ts e Shared Patterns/Autenticação)
  </read_first>
  <behavior>
    - Test 1: POST /api/v1/public/asset/:assetId/contribution sem auth, com payload válido (phone), retorna 201 com { queued, pendingId | eventId }
    - Test 2: POST /api/v1/public/asset/:assetId/contribution com assetId inexistente retorna 404
    - Test 3: POST /api/v1/public/asset/:assetId/contribution sem phone nem email retorna 400
    - Test 4: POST /api/v1/contributions/:id/review com OPERATOR + decision=APPROVED retorna 200 com { status: APPROVED, eventId }
    - Test 5: POST /api/v1/contributions/:id/review sem API key retorna 401
    - Test 6: POST /api/v1/contributions/:id/review com role READER retorna 403
    - Test 7: POST /api/v1/contributions/:id/review com pendingId de outro tenant retorna 404
  </behavior>
  <action>
    1. Em `src/routes/v1/publicRoutes.ts`, adicionar a rota pública:
       ```typescript
       import { CurationFacet } from '../../services/core-facets/CurationFacet';

       // POST /api/v1/public/asset/:assetId/contribution
       router.post('/asset/:assetId/contribution', async (req, res, next) => {
         try {
           const { assetId } = req.params;
           const { phone, email, payload } = req.body;
           const result = await CurationFacet.submitContribution({ assetId, phone, email, payload: payload ?? {} });
           return res.status(201).json({ success: true, data: result });
         } catch (err: any) {
           if (err.httpStatus) return res.status(err.httpStatus).json({ success: false, error: err.message, code: err.code });
           next(err);
         }
       });
       ```
    2. Criar `src/controllers/ContributionController.ts` (controller slim seguindo template BlindContactController):
       ```typescript
       import { Request, Response, NextFunction } from 'express';
       import { CurationFacet } from '../services/core-facets/CurationFacet';

       export class ContributionController {
         static async review(req: Request, res: Response, next: NextFunction) {
           try {
             const secureContext = {
               tenantId: (req as any).tenantId,
               role: (req as any).apiKeyRole,
               apiKeyId: (req as any).apiKeyId,
             };
             const payload = {
               pendingId: req.params.id,
               decision: req.body.decision,
               reason: req.body.reason,
             };
             const result = await CurationFacet.reviewContribution(secureContext, payload);
             return res.status(200).json({
               success: true, data: result,
               meta: { timestamp: new Date().toISOString(), facet: 'CURATION' },
             });
           } catch (err: any) {
             if (err.httpStatus) return res.status(err.httpStatus).json({ success: false, error: err.message, code: err.code });
             next(err);
           }
         }
       }
       ```
    3. Criar `src/routes/v1/contributionRoutes.ts`:
       ```typescript
       import { Router } from 'express';
       import { ContributionController } from '../../controllers/ContributionController';
       import { requireApiKey } from '../../middleware/apiKeyAuth';
       import { requireOperator } from '../../middleware/rbacGuard';
       import { tenantRateLimiter } from '../../middleware/rateLimiter';

       const router = Router();
       router.post('/:id/review', requireApiKey, tenantRateLimiter, requireOperator, ContributionController.review);
       export default router;
       ```
    4. Em `src/routes/index.ts`, adicionar mount:
       ```typescript
       import contributionRoutes from './v1/contributionRoutes';
       // ...
       router.use('/v1/contributions', contributionRoutes);
       ```
    5. Criar `tests/curation-routes.test.ts` cobrindo os 7 casos via supertest.
  </action>
  <verify>
    <automated>npm test -- tests/curation-routes.test.ts --reporter=verbose 2>&1 | tail -50 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/asset/:assetId/contribution" src/routes/v1/publicRoutes.ts` retorna >= 1
    - `grep -c "CurationFacet.submitContribution" src/routes/v1/publicRoutes.ts` retorna >= 1
    - Arquivo `src/controllers/ContributionController.ts` existe e exporta `ContributionController.review`
    - `grep -c "CurationFacet.reviewContribution" src/controllers/ContributionController.ts` retorna >= 1
    - Arquivo `src/routes/v1/contributionRoutes.ts` existe
    - `grep -c "/:id/review" src/routes/v1/contributionRoutes.ts` retorna >= 1
    - `grep -nE "requireApiKey.*tenantRateLimiter.*requireOperator" src/routes/v1/contributionRoutes.ts` encontra a chain
    - `grep -c "contributionRoutes\\|/v1/contributions" src/routes/index.ts` retorna >= 1
    - `npm test -- tests/curation-routes.test.ts` passa com >= 7 testes
    - `npm run build` compila sem erros
  </acceptance_criteria>
  <done>
    Rota pública POST /api/v1/public/asset/:assetId/contribution e rota autenticada POST /api/v1/contributions/:id/review funcionam.
  </done>
</task>

<task type="auto">
  <name>Task 4 [BLOCKING]: Aplicar schema push + regenerar Prisma client</name>
  <files>prisma/schema.prisma (no edit, apenas push)</files>
  <read_first>
    - prisma/schema.prisma (estado pós-Task 1 — confirmar models + enum presentes)
    - .env (confirmar DATABASE_URL apontando para DB de dev correto)
  </read_first>
  <action>
    Esta task é BLOCKING — deve rodar APÓS Tasks 1-3 e ANTES da verificação final.

    1. Rodar `npx prisma format` para garantir formatação consistente.
    2. Rodar `npx prisma validate` — deve retornar "schema is valid".
    3. Rodar `npx prisma db push` para aplicar o schema ao DB local. Se houver risco de data loss (não deveria, pois apenas adicionamos modelos), o comando perguntará — em ambiente CI/non-TTY usar `npx prisma db push --accept-data-loss`. Como adicionamos APENAS modelos novos (sem alterar existentes), `db push` simples deve bastar.
    4. Rodar `npx prisma generate` para regenerar o client TypeScript com os novos tipos `Contributor`, `PendingContribution`, `PendingContributionStatus`.
    5. Rodar `npm run build` — deve compilar sem erros (CurationFacet.ts agora compila com tipos corretos).
    6. Rodar `npm test` (full suite) — todos os testes da fase devem passar.

    Se `prisma db push` falhar por requerer prompt interativo: NÃO usar `--force-reset` (perderia dados). Em vez disso, criar migration explícita: `npx prisma migrate dev --name add_curation_layer`.
  </action>
  <verify>
    <automated>npx prisma validate 2>&1 | tail -5 && npx prisma db push --skip-generate 2>&1 | tail -10 && npx prisma generate 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` retorna "valid"
    - `npx prisma db push` completa sem erro (output contém "in sync" ou "Database is now in sync")
    - `npx prisma generate` completa sem erro
    - `node -e "const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); console.log(typeof p.contributor, typeof p.pendingContribution)"` imprime "object object" (clients exposed)
    - `npm run build` compila sem erros
    - `npm test` full suite passa (todas as fases anteriores e curation tests)
  </acceptance_criteria>
  <done>
    DB local sincronizado com novos modelos. Prisma client regenerado. Build verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → POST /public/asset/:id/contribution | Submissão pública sem API key — qualquer um pode enviar |
| Reviewer → POST /contributions/:id/review | Tenant autenticado revisa contribuição; tenantId DEVE vir do secureContext |
| CurationFacet → AnchorQueue | Aprovação dispara ancoragem em DLT — efeito financeiro |
| Payload JSON opaco → DB | Golden Rule: payload nunca é interpretado pelo core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Submissão pública sem auth permitindo impersonation | accept | Phone/email são identificadores fracos por design — auditor humano valida; isAuditor é opt-in pelo tenant |
| T-04-02 | Tampering | Reviewer aprova contribuição de outro tenant | mitigate | reviewContribution faz findFirst com `where: { id, tenantId }` — cross-tenant retorna NOT_FOUND |
| T-04-03 | Elevation of Privilege | READER aprova contribuição | mitigate | RBAC inline no Facet checa role !== 'ADMIN' && role !== 'OPERATOR' antes de qualquer operação |
| T-04-04 | Denial of Service | Spam de contribuições públicas | mitigate | Rate limiter global (server.ts IP-based) + tenantRateLimiter (presente no review). Para a rota pública, o IP rate limit é a primeira linha; rate limiter por contributor (ownerRef) é follow-up de Phase 4 (OPS) |
| T-04-05 | Tampering | Payload malicioso injetado no EventLog | accept | Payload é JSON opaco (Golden Rule) — core nunca interpreta. Limite de 500kb do bodyParser global protege HTTP |
| T-04-06 | Repudiation | Reviewer rejeita arbitrariamente sem rastro | mitigate | reviewedBy = apiKeyId + reviewedAt set na transação; histórico de auditLog em fase futura |
</threat_model>

<verification>
- `npm test` full suite passa
- `npm run build` compila sem erros
- `npx prisma validate` retorna valid
- DB tem tables Contributor e PendingContribution: `psql -d <db> -c "\dt"` mostra ambas
- Manual: curl POST /api/v1/public/asset/:id/contribution sem auth retorna 201 (com asset válido)
- Manual: curl POST /api/v1/contributions/:id/review com OPERATOR retorna 200
</verification>

<success_criteria>
1. CORE-05: Não-auditor submetendo contribuição cria PendingContribution com PENDING_APPROVAL
2. CORE-05: Auditor (Contributor.isAuditor=true) submetendo contribuição cria EventLog APPROVED diretamente
3. CORE-06: OPERATOR/ADMIN aprova contribuição → cria EventLog + dispara AnchorQueue
4. CORE-06: OPERATOR/ADMIN rejeita contribuição → status REJECTED, reviewedBy + reviewedAt registrados
5. CORE-06: READER recebe 403; reviewer cross-tenant recebe 404
6. Schema push aplicado, prisma client regenerado, build verde
</success_criteria>

<output>
Após completar todas as tarefas, criar `.planning/phases/01-core-gap-closure-production-hardening/01-04-SUMMARY.md` documentando:
(a) novos modelos Prisma (Contributor, PendingContribution, enum),
(b) endpoints novos com middleware chains,
(c) fluxo auditor vs não-auditor (diagrama textual ASCII).
</output>
