---
phase: 01-core-gap-closure-production-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/AnchorQueueService.ts
  - src/diamond/FacetRegistry.ts
  - src/services/core-facets/DocumentVerificationFacet.ts
  - src/routes/v1/publicRoutes.ts
  - src/services/core-facets/AlgorandAnchorFacet.ts
  - tests/document-verification.test.ts
  - tests/scheduler.test.ts
  - tests/chain-transaction-tenant.test.ts
autonomous: true
requirements:
  - SEC-04
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "Dois workers paralelos do AnchorQueueService nunca lockam o mesmo EventLog (SELECT FOR UPDATE SKIP LOCKED)"
    - "POST /api/v1/diamond { selector: 'document.verify' } retorna resultado, não 404"
    - "Resposta de DocumentVerificationFacet expõe { verified: boolean }, alinhado com testes existentes"
    - "Toda ChainTransaction criada por AlgorandAnchorFacet tem tenantId preenchido (não-nullable, sempre presente)"
  artifacts:
    - path: "src/services/AnchorQueueService.ts"
      provides: "FOR UPDATE SKIP LOCKED dentro de prisma.$transaction"
      contains: "FOR UPDATE SKIP LOCKED"
    - path: "src/diamond/FacetRegistry.ts"
      provides: "Selector document.verify registrado"
      contains: "document.verify"
    - path: "src/services/core-facets/DocumentVerificationFacet.ts"
      provides: "Interface harmonizada { verified: boolean }"
      contains: "verified"
    - path: "tests/chain-transaction-tenant.test.ts"
      provides: "Teste novo provando tenantId em ChainTransaction"
  key_links:
    - from: "src/diamond/FacetRegistry.ts"
      to: "src/services/core-facets/DocumentVerificationFacet.ts"
      via: "lambda wrapper no registry"
      pattern: "document\\.verify"
    - from: "src/services/AnchorQueueService.ts"
      to: "PostgreSQL EventLog table"
      via: "$queryRaw FOR UPDATE SKIP LOCKED dentro de $transaction"
      pattern: "FOR UPDATE SKIP LOCKED"
    - from: "src/services/core-facets/AlgorandAnchorFacet.ts"
      to: "prisma.chainTransaction.create"
      via: "campo tenantId obrigatório"
      pattern: "tenantId:"
---

<objective>
Fechar 3 gaps de produção independentes mas relacionados:
SEC-04 (lock pessimista no AnchorQueue para evitar double-processing em rolling deploy),
SEC-05 (DocumentVerificationFacet inacessível via Diamond + interface divergente dos testes),
SEC-06 (auditar e garantir tenantId em ChainTransaction).

Purpose: Sem SEC-04, dois workers podem ancorar o mesmo evento em duas txs diferentes
(perda de dinheiro irreversível). Sem SEC-05, a verificação de documentos é inacessível
do mundo exterior. Sem SEC-06, queries cross-chain por tenant ficam impossíveis.
Output: Lock distribuído seguro, document.verify reachable, ChainTransaction tenant-scoped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-gap-closure-production-hardening/01-RESEARCH.md
@.planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md
@CLAUDE.md
@src/services/AnchorQueueService.ts
@src/diamond/FacetRegistry.ts
@src/services/core-facets/DocumentVerificationFacet.ts
@src/services/core-facets/AlgorandAnchorFacet.ts
@prisma/schema.prisma

<interfaces>
<!-- Tipos relevantes para os tasks abaixo -->

From src/services/core-facets/DocumentVerificationFacet.ts (a ser harmonizado):
```typescript
export interface VerifyDocumentResponse {
  verified: boolean;        // ANTES era 'valid' — renomear
  asset: PublicAssetPanel | null;
  reason?: string;
}
export class DocumentVerificationFacet {
  static async verifyByHash(hash: string): Promise<VerifyDocumentResponse>;
}
```

From prisma/schema.prisma (model ChainTransaction):
```prisma
model ChainTransaction {
  id       String @id @default(cuid())
  tenantId String                              // já existe, NÃO nullable
  tenant   Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  txHash   String
  chain    String
  // ...demais campos
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar SELECT FOR UPDATE SKIP LOCKED em AnchorQueueService (SEC-04)</name>
  <files>src/services/AnchorQueueService.ts, tests/scheduler.test.ts</files>
  <read_first>
    - src/services/AnchorQueueService.ts (linhas 14-22 — findMany inicial; linhas 24-27 — early return; loop de processamento até linha ~124)
    - prisma/schema.prisma (model EventLog — confirmar nomes das colunas: status, dltTxId, signatureHash, tenantId, assetId)
    - tests/scheduler.test.ts (testes existentes — entender setup de prisma mock vs real DB)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção AnchorQueueService — pattern de SKIP LOCKED dentro de $transaction)
    - .planning/phases/01-core-gap-closure-production-hardening/01-RESEARCH.md (Pitfall 3 — SKIP LOCKED requer transação)
  </read_first>
  <behavior>
    - Test 1: Quando dois processQueue() rodam em paralelo (Promise.all), nenhum EventLog é processado duas vezes (verificar dltTxId final)
    - Test 2: Quando há 0 eventos pendentes, processQueue retorna { processed: 0 } sem erro
    - Test 3: Eventos com dltTxId já preenchido são pulados (não retornados pelo SELECT)
    - Test 4: Eventos com signatureHash NULL são pulados (filtro mantido)
  </behavior>
  <action>
    1. Em AnchorQueueService.processQueue(), localizar o `prisma.eventLog.findMany({ where: { status: { in: ['APPROVED', 'PENDING_FUNDS'] }, dltTxId: null, signatureHash: { not: null } }, orderBy: { id: 'asc' }, take: 10 })`.
    2. Substituir por bloco em `prisma.$transaction(async (tx) => { ... })`:
       ```typescript
       const pendingEvents = await prisma.$transaction(async (tx) => {
         const events = await tx.$queryRaw<Array<{
           id: string;
           assetId: string;
           tenantId: string;
           signatureHash: string;
         }>>`
           SELECT id, "assetId", "tenantId", "signatureHash"
           FROM "EventLog"
           WHERE status IN ('APPROVED', 'PENDING_FUNDS')
             AND "dltTxId" IS NULL
             AND "signatureHash" IS NOT NULL
           ORDER BY id ASC
           LIMIT 10
           FOR UPDATE SKIP LOCKED
         `;
         // Marcar imediatamente como PROCESSING dentro da MESMA transação,
         // garantindo que o lock seja convertido em estado persistente:
         if (events.length > 0) {
           await tx.eventLog.updateMany({
             where: { id: { in: events.map(e => e.id) }, dltTxId: null },
             data: { dltTxId: 'PROCESSING' },
           });
         }
         return events;
       });
       ```
    3. Manter o early return `if (pendingEvents.length === 0) { console.log(...); return { processed: 0, items: [] }; }`.
    4. NÃO remover o lock atômico `dltTxId: 'PROCESSING'` que ocorre dentro da transação acima — é complementar ao SKIP LOCKED (defense in depth).
    5. Manter inalterado o resto do método (loop de processamento, error handling, RetryWorker, WebhookDispatcher).
    6. Adicionar/expandir teste em tests/scheduler.test.ts cobrindo o behavior — caso de duplo-processQueue paralelo. Se o teste exigir DB real (não mock), adicionar nota e usar setup de teste integrado existente.
  </action>
  <verify>
    <automated>npm test -- tests/scheduler.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FOR UPDATE SKIP LOCKED" src/services/AnchorQueueService.ts` retorna >= 1
    - `grep -c "prisma.\\$transaction" src/services/AnchorQueueService.ts` retorna >= 1
    - `grep -c "\\$queryRaw" src/services/AnchorQueueService.ts` retorna >= 1
    - `grep -nE "findMany\\(\\{[^}]*status:.*APPROVED" src/services/AnchorQueueService.ts | grep -v "^#"` NÃO encontra o findMany original (foi substituído)
    - `grep -c "dltTxId.*PROCESSING\\|dltTxId: 'PROCESSING'" src/services/AnchorQueueService.ts` retorna >= 1 (lock complementar mantido)
    - `npm test -- tests/scheduler.test.ts` passa
    - `npm run build` compila sem erros
  </acceptance_criteria>
  <done>
    AnchorQueueService usa SKIP LOCKED dentro de $transaction; teste de concorrência prova que double-processing é impossível.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harmonizar interface DocumentVerificationFacet + registrar selector document.verify (SEC-05)</name>
  <files>src/services/core-facets/DocumentVerificationFacet.ts, src/diamond/FacetRegistry.ts, src/routes/v1/publicRoutes.ts, tests/document-verification.test.ts</files>
  <read_first>
    - src/services/core-facets/DocumentVerificationFacet.ts (interface VerifyDocumentResponse e todos os return points)
    - src/routes/v1/publicRoutes.ts (linhas 72-85 — handler que usa result.valid, precisa virar result.verified)
    - src/diamond/FacetRegistry.ts (linhas 7-18 imports; linhas 64-67 padrão de lambda; seção EVENT LOG perto da linha 33)
    - tests/document-verification.test.ts (testes existentes — confirmar que esperam { verified } e não { valid })
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seções FacetRegistry e DocumentVerificationFacet)
  </read_first>
  <behavior>
    - Test 1: DocumentVerificationFacet.verifyByHash('hash-existente') retorna { verified: true, asset: {...} }
    - Test 2: verifyByHash('hash-inexistente') retorna { verified: false, asset: null, reason: ... }
    - Test 3: verifyByHash('hash-malformado') retorna { verified: false, asset: null, reason: 'Invalid hash format' }
    - Test 4: POST /api/v1/diamond { selector: 'document.verify', payload: { hash } } retorna 200 com resultado (não 404)
  </behavior>
  <action>
    1. Em DocumentVerificationFacet.ts, localizar `export interface VerifyDocumentResponse { valid: boolean; ... }` e renomear o campo `valid` para `verified`. Atualizar TODOS os `return { valid: ... }` no arquivo para `return { verified: ... }` (incluindo as linhas ~85-86 e ~111-121).
    2. Em src/routes/v1/publicRoutes.ts (linhas ~72-85), atualizar o handler `GET /verify/document/:hash` para usar `result.verified` em vez de `result.valid`. O response payload externo permanece compatível (manter `{ valid: true/false }` na resposta HTTP pública para não quebrar API consumers — usar `result.verified` apenas internamente; OU promover `verified` na resposta se RESEARCH não indicar quebra de contrato externo). Optar por:
       ```typescript
       const result = await DocumentVerificationFacet.verifyByHash(hash);
       if (!result.verified) {
         return res.status(404).json({ verified: false, asset: null });
       }
       return res.status(200).json({ verified: true, asset: result.asset });
       ```
       (Se houver consumers públicos esperando `valid`, documentar a mudança no SUMMARY como breaking change para Fase 2 DOC-01).
    3. Em src/diamond/FacetRegistry.ts:
       a. Adicionar import: `import { DocumentVerificationFacet } from '../services/core-facets/DocumentVerificationFacet';`
       b. Adicionar uma seção comentada `// DOCUMENT VERIFICATION` (após `// EVENT LOG`) e registrar o selector:
          ```typescript
          'document.verify': (_ctx: any, payload: any) => DocumentVerificationFacet.verifyByHash(payload.hash),
          ```
    4. Atualizar tests/document-verification.test.ts se algum teste ainda espera `valid`. Adicionar (se não existir) um teste que invoca o selector via FacetRegistry/DiamondProxy diretamente provando que retorna resultado (não throw "selector not found").
  </action>
  <verify>
    <automated>npm test -- tests/document-verification.test.ts --reporter=verbose 2>&1 | tail -40 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'valid: (true|false|result\\.|boolean)' src/services/core-facets/DocumentVerificationFacet.ts` NÃO encontra retornos com campo `valid` (apenas `verified`)
    - `grep -c "verified" src/services/core-facets/DocumentVerificationFacet.ts` retorna >= 4 (interface + retornos)
    - `grep -c "'document.verify'" src/diamond/FacetRegistry.ts` retorna >= 1
    - `grep -c "DocumentVerificationFacet" src/diamond/FacetRegistry.ts` retorna >= 2 (import + uso)
    - `grep -c "result.valid" src/routes/v1/publicRoutes.ts` retorna 0 (substituído por result.verified)
    - `npm test -- tests/document-verification.test.ts` passa
    - `npm run build` compila sem erros
  </acceptance_criteria>
  <done>
    Selector `document.verify` está registrado e retorna `{ verified: boolean, asset, reason? }`. Testes existentes passam sem alteração de seu campo esperado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Garantir tenantId em ChainTransaction (SEC-06)</name>
  <files>src/services/core-facets/AlgorandAnchorFacet.ts, tests/chain-transaction-tenant.test.ts</files>
  <read_first>
    - src/services/core-facets/AlgorandAnchorFacet.ts (todos os call sites de `prisma.chainTransaction.create`)
    - prisma/schema.prisma (model ChainTransaction — confirmar tenantId não-nullable; relação com Tenant)
    - src/services/AnchorQueueService.ts (como ele invoca o adapter — passar tenantId no contexto)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção AlgorandAnchorFacet)
    - .planning/phases/01-core-gap-closure-production-hardening/01-RESEARCH.md (Wave 0 Gap — tests/chain-transaction-tenant.test.ts não existe)
  </read_first>
  <behavior>
    - Test 1: Após anchorEvent() ser chamado para um EventLog cujo tenantId='T1', o ChainTransaction criado tem tenantId='T1' (não null, não outro tenant)
    - Test 2: Tentativa de criar ChainTransaction sem tenantId via Prisma falha (validação a nível de schema — feature gratuita, mas vale assertar)
    - Test 3: Query findMany por tenantId='T1' retorna apenas as transactions desse tenant
  </behavior>
  <action>
    1. Em AlgorandAnchorFacet.ts, localizar TODOS os `prisma.chainTransaction.create({ data: {...} })`. Para cada um, verificar:
       - Se `tenantId` já é passado no objeto data → ok, garantir que vem do `event.tenantId` (não de outro source).
       - Se `tenantId` AUSENTE → adicionar `tenantId: event.tenantId` (ou parâmetro equivalente recebido do AnchorQueueService).
    2. Verificar a assinatura do método `anchorEvent(eventId, hash)` declarada em IDLTAdapter — se não recebe tenantId, ler a partir de `eventLog` no início do método via `prisma.eventLog.findUnique({ where: { id: eventId } })` e usar `eventLog.tenantId`. NÃO mudar a assinatura pública do `IDLTAdapter` para preservar compatibilidade futura (Phase 3 — Stellar/Soroban).
    3. Criar tests/chain-transaction-tenant.test.ts cobrindo o behavior. Setup: criar 2 tenants (T1, T2), 1 EventLog para cada, chamar anchorEvent (mock do Algorand SDK retornando txId fake), assertar que ChainTransaction.tenantId é o esperado.
       - Se o adapter chama serviços externos (Algorand testnet), usar mock via vi.mock('algosdk') — seguir padrão de outros tests existentes.
  </action>
  <verify>
    <automated>npm test -- tests/chain-transaction-tenant.test.ts --reporter=verbose 2>&1 | tail -40 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'chainTransaction\\.create\\b' src/services/core-facets/AlgorandAnchorFacet.ts` mostra TODOS os call sites
    - Para cada call site acima, `grep -A 10 ...` deve mostrar `tenantId:` no objeto data (confirmar manualmente todos os pontos)
    - Arquivo `tests/chain-transaction-tenant.test.ts` existe com >= 3 testes (Test 1, 2, 3 do behavior)
    - `npm test -- tests/chain-transaction-tenant.test.ts` passa
    - `npm run build` compila sem erros
  </acceptance_criteria>
  <done>
    AlgorandAnchorFacet sempre popula ChainTransaction.tenantId com o tenant do EventLog originador. Teste comprova isolamento por tenant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker A ↔ Worker B (rolling deploy) | Dois processos lendo a mesma fila — race condition em PostgreSQL |
| Diamond router → Facet | Selector inexistente retorna 404 silenciosamente — features ficam "perdidas" |
| AlgorandAnchorFacet → ChainTransaction | Schema permite NULL? (verificar) — adapter precisa popular tenantId sempre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AnchorQueueService.processQueue (race) | mitigate | SELECT FOR UPDATE SKIP LOCKED dentro de $transaction; manter dltTxId='PROCESSING' como defense-in-depth |
| T-02-02 | Information Disclosure | DocumentVerificationFacet inacessível | mitigate | Registrar selector document.verify no FacetRegistry; harmonizar response shape com testes ({verified}) |
| T-02-03 | Information Disclosure | ChainTransaction sem tenantId → cross-tenant leak em queries | mitigate | Garantir tenantId sempre populado pelo adapter; teste de regressão |
| T-02-04 | Denial of Service | $transaction longo bloqueia outros writers | accept | Transação envolve apenas SELECT + UPDATE marcador em ≤10 rows; loop de anchoring fica fora da $transaction |
</threat_model>

<verification>
- `npm test` passa para tests/scheduler.test.ts, tests/document-verification.test.ts, tests/chain-transaction-tenant.test.ts
- `npm run build` compila sem erros TypeScript
- `grep -c "FOR UPDATE SKIP LOCKED" src/services/AnchorQueueService.ts` >= 1
- `grep -c "'document.verify'" src/diamond/FacetRegistry.ts` >= 1
- Todos os call sites de chainTransaction.create populam tenantId (inspeção manual)
</verification>

<success_criteria>
1. SEC-04: Test de concorrência (2 processQueue paralelos) prova que nenhum EventLog é processado duas vezes
2. SEC-05: POST /api/v1/diamond { selector: 'document.verify' } retorna resultado, não 404
3. SEC-05: Interface { verified: boolean } alinhada com testes existentes
4. SEC-06: Toda ChainTransaction criada por AlgorandAnchorFacet tem tenantId do tenant correto
</success_criteria>

<output>
Após completar todas as tarefas, criar `.planning/phases/01-core-gap-closure-production-hardening/01-02-SUMMARY.md` documentando: (a) query SQL final do SKIP LOCKED, (b) lista de selectors antes/depois, (c) auditoria dos call sites de chainTransaction.create.
</output>
