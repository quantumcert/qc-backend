---
phase: 01-core-gap-closure-production-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/core-facets/LifecycleFacet.ts
  - src/services/core-facets/TransferRegistryFacet.ts
  - src/controllers/TransferController.ts
  - src/routes/v1/assetRoutes.ts
  - src/services/SchedulerService.ts
  - src/controllers/WebhookController.ts
  - src/server.ts
  - tests/lifecycle-diamond.test.ts
  - tests/transfer-rest.test.ts
  - tests/webhook.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04

must_haves:
  truths:
    - "LifecycleFacet.transition rejeita BURNED → ACTIVE com 422 / STATE_TRANSITION_FORBIDDEN"
    - "PATCH /api/v1/assets/:assetId/transfer existe, autenticado, e chama TransferRegistryFacet.initiateTransfer"
    - "SchedulerService está rodando AnchorQueueService a cada N segundos quando server inicia"
    - "SchedulerService tem job processando WebhookInbox (status PENDING → APPROVED) a cada N segundos"
    - "MP_WEBHOOK_SECRET é exigido em produção; webhook com HMAC inválido retorna 401"
  artifacts:
    - path: "src/controllers/TransferController.ts"
      provides: "Controller slim para PATCH /assets/:id/transfer"
      contains: "TransferRegistryFacet.initiateTransfer"
    - path: "src/routes/v1/assetRoutes.ts"
      provides: "Rota PATCH /:assetId/transfer registrada"
      contains: "/:assetId/transfer"
    - path: "src/services/SchedulerService.ts"
      provides: "Cron jobs para AnchorQueue + WebhookInbox"
      contains: "WEBHOOK_INBOX_INTERVAL_SECONDS"
  key_links:
    - from: "src/routes/v1/assetRoutes.ts"
      to: "src/controllers/TransferController.ts"
      via: "router.patch chain"
      pattern: "TransferController\\.initiateTransfer"
    - from: "src/services/SchedulerService.ts"
      to: "WebhookInbox processor (BillingFacet ou equivalente)"
      via: "cron.schedule com isRunning guard"
      pattern: "cron\\.schedule"
---

<objective>
Validar e completar o "core gap closure" — código que já existe parcialmente:
CORE-01 (LifecycleFacet — verificar e adicionar testes de regressão para transições terminais),
CORE-02 (rota REST PATCH /assets/:id/transfer — não existe, criar),
CORE-03 (SchedulerService — verificar wiring),
CORE-04 (MercadoPago webhook — verificar wiring + adicionar processador de WebhookInbox se ausente).

Purpose: O codebase tem implementações parciais; este plano fecha as últimas pontas
para que todas as features existentes sejam realmente alcançáveis e auto-disparáveis.
Output: Rota REST de transfer, scheduler com job de WebhookInbox, testes de regressão
para state machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-gap-closure-production-hardening/01-RESEARCH.md
@.planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md
@CLAUDE.md
@src/services/core-facets/LifecycleFacet.ts
@src/services/core-facets/TransferRegistryFacet.ts
@src/services/SchedulerService.ts
@src/controllers/WebhookController.ts
@src/routes/v1/assetRoutes.ts

<interfaces>
<!-- Contratos existentes que serão consumidos pelos novos artefatos -->

From src/services/core-facets/TransferRegistryFacet.ts:
```typescript
export class TransferRegistryFacet {
  static async initiateTransfer(
    secureContext: { tenantId: string; role: string; apiKeyId: string },
    payload: { assetId: string; toOwner: string; reason?: string }
  ): Promise<{ paymentLink: string; transferId: string }>;
}
```

From src/middleware (chain padrão para rota autenticada):
```typescript
requireApiKey       // injeta req.tenantId, req.apiKeyId, req.apiKeyRole
requireIdempotency  // verifica X-Idempotency-Key
tenantRateLimiter   // rate limit por tenant
requireOperator     // ADMIN ou OPERATOR
```

From src/controllers/BlindContactController.ts (template slim para TransferController):
```typescript
// Padrão: extrair secureContext de req.tenantId/req.apiKeyId/req.apiKeyRole,
// validar payload, chamar Facet, retornar { success, data, meta }.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar PATCH /api/v1/assets/:assetId/transfer (CORE-02)</name>
  <files>src/controllers/TransferController.ts, src/routes/v1/assetRoutes.ts, tests/transfer-rest.test.ts</files>
  <read_first>
    - src/controllers/BlindContactController.ts (linhas 1-41 — template de controller slim)
    - src/services/core-facets/TransferRegistryFacet.ts (assinatura de initiateTransfer)
    - src/routes/v1/assetRoutes.ts (linha 204 — padrão de PATCH com middleware chain)
    - src/middleware/apiKeyAuth.ts e src/middleware/rbacGuard.ts (entender requireOperator e requireApiKey)
    - tests/transfer-diamond.test.ts (testes existentes via Diamond — adaptar para REST)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção assetRoutes.ts e Shared Patterns/Autenticação)
  </read_first>
  <behavior>
    - Test 1: PATCH /api/v1/assets/:id/transfer com API key OPERATOR válida e payload {toOwner} retorna 200 com { paymentLink, transferId }
    - Test 2: PATCH sem X-API-Key retorna 401
    - Test 3: PATCH com role READER retorna 403
    - Test 4: PATCH sem X-Idempotency-Key retorna 400 (idempotency required)
    - Test 5: PATCH com assetId de outro tenant retorna 404 (isolamento)
  </behavior>
  <action>
    1. Criar src/controllers/TransferController.ts com classe `TransferController` e método estático `initiateTransfer(req, res, next)`:
       ```typescript
       import { Request, Response, NextFunction } from 'express';
       import { TransferRegistryFacet } from '../services/core-facets/TransferRegistryFacet';

       export class TransferController {
         static async initiateTransfer(req: Request, res: Response, next: NextFunction) {
           try {
             const secureContext = {
               tenantId: (req as any).tenantId,
               role: (req as any).apiKeyRole,
               apiKeyId: (req as any).apiKeyId,
             };
             const payload = {
               assetId: req.params.assetId,
               toOwner: req.body.toOwner,
               reason: req.body.reason,
             };
             const result = await TransferRegistryFacet.initiateTransfer(secureContext, payload);
             return res.status(200).json({
               success: true,
               data: result,
               meta: { timestamp: new Date().toISOString(), facet: 'TRANSFER' },
             });
           } catch (err: any) {
             if (err.code === 'ASSET_NOT_FOUND') return res.status(404).json({ success: false, error: err.message });
             if (err.code === 'INSUFFICIENT_PERMISSIONS') return res.status(403).json({ success: false, error: err.message });
             if (err.httpStatus) return res.status(err.httpStatus).json({ success: false, error: err.message, code: err.code });
             next(err);
           }
         }
       }
       ```
    2. Em src/routes/v1/assetRoutes.ts:
       - Adicionar import: `import { TransferController } from '../../controllers/TransferController';`
       - Adicionar a rota seguindo o padrão de `/:id/owners` (linha 204):
         ```typescript
         router.patch('/:assetId/transfer',
           requireApiKey, requireIdempotency, tenantRateLimiter, requireOperator,
           TransferController.initiateTransfer);
         ```
    3. Criar tests/transfer-rest.test.ts cobrindo o behavior. Reutilizar setup de testes existentes (e.g., supertest com app de teste).
  </action>
  <verify>
    <automated>npm test -- tests/transfer-rest.test.ts --reporter=verbose 2>&1 | tail -40 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/controllers/TransferController.ts` existe e exporta `TransferController` com método `initiateTransfer`
    - `grep -c "TransferRegistryFacet.initiateTransfer" src/controllers/TransferController.ts` retorna >= 1
    - `grep -c "/:assetId/transfer" src/routes/v1/assetRoutes.ts` retorna >= 1
    - `grep -nE "patch.*:assetId/transfer" src/routes/v1/assetRoutes.ts` mostra a chain `requireApiKey, requireIdempotency, tenantRateLimiter, requireOperator`
    - `grep -c "TransferController" src/routes/v1/assetRoutes.ts` retorna >= 2 (import + uso)
    - Arquivo `tests/transfer-rest.test.ts` existe com >= 5 testes
    - `npm test -- tests/transfer-rest.test.ts` passa
  </acceptance_criteria>
  <done>
    Rota REST `PATCH /api/v1/assets/:assetId/transfer` funciona como wrapper REST do `transfer.initiate` Diamond selector.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar regression tests para LifecycleFacet (CORE-01)</name>
  <files>src/services/core-facets/LifecycleFacet.ts, tests/lifecycle-diamond.test.ts</files>
  <read_first>
    - src/services/core-facets/LifecycleFacet.ts (linhas 16-20 — TRANSITION_RULES; método transition)
    - tests/lifecycle-diamond.test.ts (testes existentes — confirmar quais transições já estão cobertas)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção LifecycleFacet — análise de TRANSITION_RULES)
    - .planning/phases/01-core-gap-closure-production-hardening/01-RESEARCH.md (CORE-01 — análise: ARCHIVED e BURNED como terminais corretos)
  </read_first>
  <behavior>
    - Test 1: transition de BURNED → ACTIVE retorna erro com code STATE_TRANSITION_FORBIDDEN e httpStatus 422
    - Test 2: transition de ARCHIVED → ACTIVE retorna erro STATE_TRANSITION_FORBIDDEN/422
    - Test 3: transition de ACTIVE → ACTIVE (no-op) retorna erro STATE_TRANSITION_FORBIDDEN
    - Test 4: transition de DRAFT → ACTIVE com role OPERATOR funciona
    - Test 5: transition de DRAFT → BURNED rejeita (ARCHIVED/BURNED só a partir de ACTIVE)
    - Test 6: AWAITING_PAYMENT → ACTIVE NÃO é permitido via LifecycleFacet (apenas via BillingFacet)
  </behavior>
  <action>
    1. Verificar em LifecycleFacet.ts que `TRANSITION_RULES` corresponde ao mapa documentado em PATTERNS.md (DRAFT, ACTIVE, SUSPENDED apenas; ARCHIVED/BURNED ausentes = terminais; AWAITING_PAYMENT ausente = controlado por BillingFacet).
    2. Se `TRANSITION_RULES` divergir, NÃO alterar — em vez disso, registrar discrepância no SUMMARY ao final. Para esta fase, comportamento atual está correto conforme análise de research.
    3. Em tests/lifecycle-diamond.test.ts, adicionar (se ausentes) os 6 casos de teste do behavior. Verificar que `err.code === 'STATE_TRANSITION_FORBIDDEN'` e `err.httpStatus === 422` (ou que a resposta HTTP correspondente seja 422 quando a chamada vai via Diamond).
    4. Comentar no topo de TRANSITION_RULES (se não houver comentário) reforçando a intenção arquitetural:
       ```typescript
       // ARCHIVED e BURNED são estados TERMINAIS (ausentes do mapa = sem saída).
       // AWAITING_PAYMENT → ACTIVE é controlado por BillingFacet, NÃO por esta rota.
       ```
  </action>
  <verify>
    <automated>npm test -- tests/lifecycle-diamond.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TRANSITION_RULES" src/services/core-facets/LifecycleFacet.ts` >= 1
    - `grep -c "BURNED" tests/lifecycle-diamond.test.ts` >= 1 (caso de regressão presente)
    - `grep -c "STATE_TRANSITION_FORBIDDEN" tests/lifecycle-diamond.test.ts` >= 1
    - `grep -cE "AWAITING_PAYMENT.*BillingFacet|BillingFacet.*AWAITING_PAYMENT|PAYMENT_PROCESSING" tests/lifecycle-diamond.test.ts` >= 1 (teste verifica que AWAITING_PAYMENT → ACTIVE é rejeitado com mensagem mencionando BillingFacet ou PAYMENT_PROCESSING)
    - `npm test -- tests/lifecycle-diamond.test.ts` passa com pelo menos 6 testes cobrindo o behavior
  </acceptance_criteria>
  <done>
    LifecycleFacet tem testes de regressão provando que estados terminais não aceitam saída.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar SchedulerService + adicionar processador de WebhookInbox (CORE-03, CORE-04)</name>
  <files>src/services/SchedulerService.ts, src/controllers/WebhookController.ts, src/server.ts, tests/webhook.test.ts</files>
  <read_first>
    - src/services/SchedulerService.ts (todos os cron.schedule existentes; padrão isRunning guard linhas 18-34)
    - src/server.ts (verificar se SchedulerService é chamado após app.listen())
    - src/controllers/WebhookController.ts (entender WebhookInbox.create — como persiste registros PENDING)
    - prisma/schema.prisma (model WebhookInbox — ver status, payload, createdAt)
    - src/services/core-facets/BillingFacet.ts SE existir (procurar método processInbox/processWebhookInbox)
    - tests/webhook.test.ts (testes existentes para webhook MP)
    - .planning/phases/01-core-gap-closure-production-hardening/01-PATTERNS.md (seção SchedulerService — pattern de cron job com isRunning guard)
  </read_first>
  <action>
    1. Confirmar em src/server.ts que `SchedulerService.start()` é chamado após `app.listen()`. Se não estiver, adicionar.
    2. Em src/services/SchedulerService.ts, verificar se já existe um cron job processando WebhookInbox. Se NÃO existir:
       a. Procurar por método de processamento em BillingFacet ou criar `WebhookInboxProcessor` slim:
          - Buscar em `WebhookInbox` registros com `status: 'PENDING'`, ordenados por createdAt
          - Para cada registro: parsear payload, chamar BillingFacet.confirmPayment ou método análogo, marcar status APPROVED ou FAILED
          - Se BillingFacet já tem método de processamento, apenas chamá-lo no cron
       b. Adicionar cron job no SchedulerService (logo após o job AnchorQueue, mesmo padrão isRunning guard):
          ```typescript
          let webhookInboxRunning = false;
          const webhookInboxInterval = parseInt(process.env.WEBHOOK_INBOX_INTERVAL_SECONDS ?? '30', 10);
          const webhookInboxPattern = `*/${webhookInboxInterval} * * * * *`;
          cron.schedule(webhookInboxPattern, async () => {
            if (webhookInboxRunning) {
              // TODO(OPS-03): substituir console.log por logger estruturado (Phase 4)
              console.log('[Scheduler] WebhookInbox already running, skipping this cycle.');
              return;
            }
            webhookInboxRunning = true;
            try {
              await BillingFacet.processWebhookInbox(); // ou WebhookInboxProcessor.processPending()
            } catch (err) {
              // TODO(OPS-03): substituir console.error por logger estruturado (Phase 4)
              console.error('[Scheduler] WebhookInbox error:', err);
            } finally {
              webhookInboxRunning = false;
            }
          });
          ```
       c. Se BillingFacet NÃO tem método `processWebhookInbox()`: implementá-lo nesta task. Lógica real obrigatória — busca registros `WebhookInbox` com `status: 'PENDING'` (ordenados por createdAt, batch de 10), parseia `payload` JSON, chama o handler de domínio (e.g., `BillingFacet.confirmPayment(payload)` ou método análogo já existente para confirmar pagamento e transicionar Asset de AWAITING_PAYMENT → ACTIVE), atualiza `WebhookInbox.status` para `PROCESSED` em sucesso ou `FAILED` (com `errorMessage`) em exceção. Usar `prisma.$transaction` para garantir atomicidade entre update do inbox e mudança de estado do Asset. Stub não é aceitável — o critério de sucesso do ROADMAP exige SchedulerService processando WebhookInbox de verdade. Se um método já existir em BillingFacet com nome diferente, apenas chamar; se não existir, criá-lo aqui. Logging de erro via `console.error` é débito técnico para OPS-03 (Phase 4) — adicionar comentário inline `// TODO(OPS-03): substituir por logger estruturado`.
    3. Em src/server.ts, garantir `MP_WEBHOOK_SECRET` está em REQUIRED_ENV_VARS quando NODE_ENV=production (mesmo padrão do Plan 01 Task 3 — coordenar para não duplicar). Se já adicionado pelo Plan 01, não duplicar.
    4. Em tests/webhook.test.ts, adicionar (se ausentes) testes:
       - Webhook com HMAC inválido retorna 401
       - Webhook com HMAC válido cria registro WebhookInbox com status PENDING
    5. Em tests/scheduler.test.ts, adicionar (se ausente) teste que confirma que o cron job de WebhookInbox está registrado (verificar via mock/spy de cron.schedule).
  </action>
  <verify>
    <automated>npm test -- tests/webhook.test.ts tests/scheduler.test.ts --reporter=verbose 2>&1 | tail -50 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WebhookInbox\\|webhookInbox" src/services/SchedulerService.ts` retorna >= 1 (job adicionado)
    - `grep -c "WEBHOOK_INBOX_INTERVAL_SECONDS" src/services/SchedulerService.ts` retorna >= 1
    - `grep -c "SchedulerService" src/server.ts` retorna >= 1 (start() é chamado)
    - `grep -c "MP_WEBHOOK_SECRET" src/server.ts` retorna >= 1 (env var validada em produção)
    - `grep -nE "cron\\.schedule" src/services/SchedulerService.ts | wc -l` retorna >= 2 (AnchorQueue + WebhookInbox)
    - `npm test -- tests/webhook.test.ts` passa
    - `npm run build` compila sem erros
  </acceptance_criteria>
  <done>
    SchedulerService roda no startup; AnchorQueue e WebhookInbox têm cron jobs ativos com isRunning guard.
    Webhook MP rejeita HMAC inválido. MP_WEBHOOK_SECRET obrigatório em produção.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente HTTP → PATCH /assets/:id/transfer | Tenant autenticado tenta mudar ownership; tenantId DEVE vir do secureContext |
| MercadoPago webhook → /api/v1/webhooks/mercadopago | External callback sem API key; HMAC obrigatório |
| LifecycleFacet caller → state machine | Callers podem tentar transições inválidas — facet DEVE rejeitar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | TransferController.initiateTransfer | mitigate | requireApiKey + requireOperator + secureContext extraído de req (não body) |
| T-03-02 | Tampering | LifecycleFacet aceitando transição BURNED→ACTIVE | mitigate | TRANSITION_RULES não inclui saída de BURNED; teste de regressão garante |
| T-03-03 | Spoofing | MercadoPago webhook | mitigate | HMAC SHA-256 com timing-safe compare (já implementado); MP_WEBHOOK_SECRET obrigatório em produção |
| T-03-04 | Denial of Service | WebhookInbox crescendo sem processador | mitigate | Cron job com isRunning guard processa inbox a cada 30s |
| T-03-05 | Repudiation | Falha no scheduler silenciosa | accept | Logs estruturados em fase OPS-03 (Phase 4); nesta fase, console.error é suficiente |
</threat_model>

<verification>
- `npm test` passa para tests/transfer-rest.test.ts, tests/lifecycle-diamond.test.ts, tests/webhook.test.ts, tests/scheduler.test.ts
- `npm run build` compila sem erros
- Manual: hit em PATCH /api/v1/assets/:id/transfer com curl + API key OPERATOR retorna 200
- Manual: cron jobs ativos visíveis em logs do servidor após startup
</verification>

<success_criteria>
1. CORE-01: BURNED → ACTIVE rejeitado com 422 (testado)
2. CORE-02: PATCH /api/v1/assets/:assetId/transfer responde com paymentLink e transferId
3. CORE-03: SchedulerService inicia com server.ts e roda AnchorQueueService periodicamente
4. CORE-04: SchedulerService processa WebhookInbox de verdade (registros PENDING → PROCESSED via BillingFacet, com FAILED em caso de erro); webhook MP rejeita HMAC inválido; MP_WEBHOOK_SECRET obrigatório em produção
</success_criteria>

<output>
Após completar todas as tarefas, criar `.planning/phases/01-core-gap-closure-production-hardening/01-03-SUMMARY.md` documentando:
(a) novo controller TransferController + middleware chain,
(b) lista de cron jobs ativos pós-fase,
(c) estado do WebhookInbox processor (implementado completo: query PENDING → handler → PROCESSED/FAILED).
</output>
