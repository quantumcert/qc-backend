---
phase: 04-b2b-admin-operations-console
plan: 06
type: execute
wave: 4
depends_on:
  - 04-01-PLAN.md
  - 04-04-PLAN.md
  - 04-05-PLAN.md
files_modified:
  - src/services/core-facets/TenantUserFacet.ts
  - src/services/core-facets/TenantQuantumBackfillFacet.ts
  - src/scripts/backfill-tenant-quantum.ts
  - src/controllers/TenantUserController.ts
  - src/routes/v1/userRoutes.ts
  - tests/tenant-backfill.test.ts
  - tests/tenant-user-contracts.test.ts
  - ../qc-dashboard/server/adminRouter.ts
  - ../qc-dashboard/server/services/qcBackendClient.ts
  - ../qc-dashboard/server/routers.ts
  - ../qc-dashboard/client/src/pages/admin/platform/TenantDetail.tsx
  - ../qc-dashboard/client/src/pages/admin/platform/TenantUsersPanel.tsx
  - ../qc-dashboard/server/wallet.credits.test.ts
  - ../qc-dashboard/server/assets.test.ts
autonomous: true
requirements:
  - ADMIN-08
  - ADMIN-09
  - ID-01
  - ID-02
  - ID-03
  - ID-04
  - ID-05
  - ID-06

must_haves:
  truths:
    - "D-19/D-21: backfill completo, Tenant Quantum e cutover B2C sao Phase 4"
    - "D-20: associar usuarios B2C, dependentes, pets/objetos/assets, creditos e saldo QTAG ao Tenant Quantum quando aplicavel"
    - "D-22: migracao deve ter dry-run, relatorio, execucao idempotente, checkpoints e audit por lote"
    - "Regra travada: registro/compra B2C altera creditos, nao wallet balance"
  artifacts:
    - path: "src/scripts/backfill-tenant-quantum.ts"
      provides: "dry-run/execute/report de migracao B2C"
      contains: "Tenant Quantum"
    - path: "src/services/core-facets/TenantQuantumBackfillFacet.ts"
      provides: "logica idempotente de migracao e reconciliacao"
      contains: "legacyDashboardUserId"
    - path: "../qc-dashboard/server/wallet.credits.test.ts"
      provides: "regressao da regra credito-only no cutover"
      contains: "creditsBalance"
  key_links:
    - from: "../qc-dashboard/drizzle/schema.ts"
      to: "src/services/core-facets/TenantQuantumBackfillFacet.ts"
      via: "legacyDashboardUserId, legacyOpenId, guardianId"
      pattern: "dashboard DB -> backend canonical users"
    - from: "src/services/core-facets/TenantQuantumBackfillFacet.ts"
      to: "src/services/core-facets/CreditLedgerFacet.ts"
      via: "migrate B2C credits into ledger"
      pattern: "credit continuity"
---

<objective>
Criar/garantir o Tenant Quantum, executar migracao B2C completa do banco do
dashboard para usuarios tenant-scoped no backend, criar contratos operacionais
para Platform Admin visualizar/criar/editar usuarios por tenant, reconciliar
ownership/creditos/QTAGs e cortar leituras/escritas B2C do dashboard para
contratos canonicos do backend.

Purpose: unificar a visibilidade operacional antes da Phase 5 B2B externa e
eliminar a duplicidade critica entre banco do dashboard e backend para dominio
B2C.
Output: backfill idempotente, relatorios, contratos backend de usuario, CRUD
operacional de usuarios por tenant para Platform Admin e cutover dashboard para
backend.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-b2b-admin-operations-console/04-SPEC.md
@.planning/phases/04-b2b-admin-operations-console/04-CONTEXT.md
@.planning/phases/04-b2b-admin-operations-console/04-RESEARCH.md
@.planning/phases/04-b2b-admin-operations-console/04-PATTERNS.md
@../qc-dashboard/drizzle/schema.ts
@../qc-dashboard/server/routers.ts
@../qc-dashboard/server/wallet.credits.test.ts
@../qc-dashboard/server/services/qcBackendClient.ts
@src/services/core-facets/CreditLedgerFacet.ts
@src/services/core-facets/QTagFulfillmentFacet.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Criar Tenant Quantum e contratos canonicos de usuario</name>
  <files>src/controllers/TenantUserController.ts, src/routes/v1/userRoutes.ts, src/services/core-facets/TenantUserFacet.ts, tests/tenant-user-contracts.test.ts</files>
  <read_first>
    - prisma/schema.prisma
    - src/services/core-facets/TenantManagementFacet.ts
    - src/routes/index.ts
    - ../qc-dashboard/server/routers.ts
  </read_first>
  <action>
    1. Criar helper idempotente `ensureTenantQuantum` com slug fixo aprovado
       `quantum-cert-platform`, status `ACTIVE` e `targetChain=STELLAR`.
    2. Criar `TenantUserFacet` ou equivalente para:
       `getCurrentUser`, `upsertB2CUser`, `listDependents`,
       `createDependent`, `updateProfile`, `linkExternalIdentity`.
    3. No mesmo contrato, expor operacoes Platform Admin tenant-scoped:
       `listTenantUsers`, `getTenantUser`, `createTenantUser`,
       `updateTenantUser`, `setTenantUserStatus`, `assignTenantUserRole`,
       `listTenantUserAssets` e `getTenantUserProfileAssetState`.
    4. Garantir que Platform Admin pode operar qualquer tenant autorizado,
       enquanto Tenant Admin self-service completo de convites/operadores fica
       para Phase 5.
    5. Garantir que B2C users vivem sob Tenant Quantum e nao viram tenants.
    6. Preservar `legacyDashboardUserId`, `legacyOpenId`, email, phone, CPF,
       role, guardian/dependent graph e metadados medicos/emergencia quando
       presentes.
    7. Toda criacao/edicao/status/role de usuario deve gerar audit event com
       actor, tenant, user alvo e payload hash/referencia.
    8. Criar testes de contratos para usuario, dependente, external identity,
       CRUD Platform Admin e visibilidade de Asset/profile.
  </action>
  <verify>
    <automated>npm test -- --run tests/tenant-user-contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tenant Quantum e criado/garantido idempotentemente com slug `quantum-cert-platform` e `targetChain=STELLAR`
    - Consumidores B2C ficam como `TenantUser`/membership do Tenant Quantum
    - Dependentes preservam relacao com guardiao
    - Platform Admin lista/cria/edita usuarios por tenant e ve estado de Asset de perfil/Assets associados
    - Tenant Admin self-service completo de equipe permanece explicitamente fora desta tarefa
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Implementar backfill dry-run/execute com relatorio</name>
  <files>src/services/core-facets/TenantQuantumBackfillFacet.ts, src/scripts/backfill-tenant-quantum.ts, tests/tenant-backfill.test.ts</files>
  <read_first>
    - ../qc-dashboard/drizzle/schema.ts
    - src/services/core-facets/TenantUserFacet.ts
    - src/services/core-facets/CreditLedgerFacet.ts
    - src/services/core-facets/QTagFulfillmentFacet.ts
    - prisma/schema.prisma
  </read_first>
  <action>
    1. Criar `TenantQuantumBackfillFacet` com mesmo motor para `dryRun` e
       `execute`.
    2. Ler usuarios/dependentes do dashboard em batches estaveis por id.
    3. Upsert por `legacyDashboardUserId`, `legacyOpenId`, email/document quando
       seguro. Conflitos devem ir ao relatorio, nao ser resolvidos por chute.
    4. Criar `MigrationRun`, checkpoints e records por lote, com checksum,
       counts source/destination, errors, warnings, unresolved ownerRefs e
       credit-source gaps.
    5. Reconciliar `Owner.ownerRef` com usuario canonico quando bater openId,
       legacy aliases, email/document permitido, preservando `ownerRef` legado.
    6. Reconciliar transferencias B2C existentes: remetente/destinatario devem
       apontar para `TenantUser`/Asset de perfil quando conhecidos; destinatario
       por CPF desconhecido vira pending recipient sob Tenant Quantum, nao tenant.
    7. Migrar creditos B2C duraveis disponiveis para `CreditLedgerEntry` sem
       tocar wallet financial balance. Se fonte for fallback/local, registrar
       gap/assumption no report.
    8. Migrar saldo QTAG disponivel quando houver fonte duravel; senao registrar
       gap operacional.
    9. Script `src/scripts/backfill-tenant-quantum.ts` deve suportar:
       `--dry-run`, `--execute`, `--batch-size`, `--resume`, `--report-json`.
  </action>
  <verify>
    <automated>npm test -- --run tests/tenant-backfill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run nao grava alteracoes de dominio, mas grava/retorna relatorio
    - Execute e idempotente e retomavel por checkpoint
    - Duplicados/conflitos aparecem no report
    - Transferencias preservam historico e resolvem ownership para TenantUser/Profile Asset quando possivel
    - Credit/QTAG continuity preserva a regra de creditos, nao wallet balance
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 3: Cortar dashboard B2C para contratos canonicos do backend</name>
  <files>../qc-dashboard/server/services/qcBackendClient.ts, ../qc-dashboard/server/routers.ts, ../qc-dashboard/server/wallet.credits.test.ts, ../qc-dashboard/server/assets.test.ts, ../qc-dashboard/server/auth.test.ts</files>
  <read_first>
    - ../qc-dashboard/server/routers.ts
    - ../qc-dashboard/server/services/qcBackendClient.ts
    - ../qc-dashboard/server/wallet.credits.test.ts
    - ../qc-dashboard/drizzle/schema.ts
  </read_first>
  <action>
    1. Adicionar `QCBackendClient.users.*`, `dependents.*`, `credits.*`,
       `assets.*` e `admin.tenantUsers.*` para contratos canonicos do backend.
    2. Alterar routers B2C do dashboard para ler/escrever usuario, perfil,
       dependentes, creditos e ownership via backend quando flag/cutover ativo.
       Banco local deve permanecer para sessao/preferencias apenas.
    3. Preservar fallback operacional somente quando explicitamente marcado como
       local/demo e sem fingir que e fonte canonica.
    4. Atualizar testes de auth/profile/dependents/assets/wallet para provar:
       - local user session continua funcionando;
       - domain write vai para backend;
       - compra/registro altera `creditsBalance`/ledger, nao `balance`;
       - ownership resolve usuario canonico e preserva legado quando necessario.
       - transferencia usa usuario canonico/profile Asset para origem/destino
         quando disponivel e mantem pendencia segura por CPF hasheado quando nao.
    5. Expor a aba Team/Usuarios do Tenant Detail usando contratos reais para
       listar/criar/editar usuario, status, role, identidade externa, Asset de
       perfil e Assets associados.
    6. Adicionar comando/nota operacional para executar cutover somente depois
       de dry-run aprovado e execute concluido.
  </action>
  <verify>
    <automated>cd ../qc-dashboard && pnpm test -- wallet assets auth</automated>
    <automated>cd ../qc-dashboard && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard B2C escreve usuarios/dependentes/creditos/assets via backend
    - DB local nao e mais fonte canonica de dominio B2C
    - Platform Admin consegue operar usuarios/equipe por tenant sem depender de mock silencioso
    - Transferencia B2C nao cria Tenant para usuario final; cria/usa TenantUser e vinculo de perfil
    - Regra credit-only permanece verde
    - Cutover tem gate operacional claro apos report de migracao
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

- Boundary A: dashboard local DB -> backend canonical user store.
- Boundary B: loose ownerRef -> canonical TenantUser link.
- Boundary C: migration execution -> existing production data.
- Boundary D: dashboard cutover -> backend contracts.

## Threats

- **HIGH: Usuario B2C vira tenant indevidamente.** Mitigation:
  `ensureTenantQuantum` e memberships sob Tenant Quantum; testes de contrato.
- **HIGH: Backfill duplica ou sobrescreve usuario errado.** Mitigation:
  upsert por chaves legacy/naturais seguras, conflict report e idempotency keys.
- **HIGH: OwnerRef associado ao usuario errado.** Mitigation:
  reconciliar somente matches confiaveis; preservar `Owner.ownerRef` legado.
- **HIGH: Credit balance migra para wallet financeira.** Mitigation:
  usar `CreditLedgerEntry`; testes preservam "nao mexer no saldo, somente no credito".
- **MEDIUM: Cutover executado antes de report aprovado.** Mitigation:
  script dry-run/execute separado, checkpoints e gate operacional documentado.
</threat_model>

<verification>

```bash
npm test -- --run tests/tenant-user-contracts.test.ts tests/tenant-backfill.test.ts
cd ../qc-dashboard && pnpm test -- wallet assets auth
cd ../qc-dashboard && pnpm check
```

</verification>

<success_criteria>

- Tenant Quantum existe como tenant canonico B2C.
- Backfill completo tem dry-run, execute, resume, relatorio e auditoria.
- Dashboard B2C corta dominio para backend mantendo sessao/preferencias locais.
</success_criteria>
