---
phase: 03-pluggable-dlt-workers-stellar-soroban-priority
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - ../qc-dashboard/server/routers.ts
  - ../qc-dashboard/server/services/qcBackendClient.ts
  - ../qc-dashboard/client/src/pages/VerifyAsset.tsx
  - ../qc-dashboard/server/verify.asset.test.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-HUMAN-UAT.md
autonomous: false
requirements:
  - DLT-01
  - DLT-03
  - DLT-04
deferred_requirements_documented:
  - DLT-02
  - DLT-05

must_haves:
  truths:
    - "D-17/D-18/D-18A: dashboard mostra card Verificação Blockchain para qualquer blockchain.chain; Stellar é apenas o caso UAT"
    - "D-19: dashboard mantem fallback local/demo quando qc-backend falha"
    - "D-02/D-03: DLT-02 Solana e DLT-05 lastScannedBlock sao tratados como deferidos por SPEC, nao implementados"
    - "Acceptance: UAT humano deve registrar tx Stellar, explorerUrl e prova get_anchor_hash"
  artifacts:
    - path: "../qc-dashboard/server/routers.ts"
      provides: "assets.publicVerify propaga blockchain proof para o frontend"
      contains: "blockchain"
    - path: "../qc-dashboard/client/src/pages/VerifyAsset.tsx"
      provides: "card Verificação Blockchain genérico por chain, com link de explorer opcional"
      contains: "Verificação Blockchain"
    - path: ".planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-HUMAN-UAT.md"
      provides: "roteiro UAT hackathon com evidencias manuais"
      contains: "Stellar Expert"
---

<objective>
Fechar a experiencia demonstravel do hackathon: o dashboard renderiza a prova
blockchain vinda do backend, o UAT humano captura evidencias reais em testnet, e o
escopo deferido de Solana/lastScannedBlock fica documentado para nao parecer
entrega incompleta acidental.

Purpose: transformar a prova backend em uma experiencia verificavel por juiz e
preservar rastreabilidade do escopo que ficou fora do slice.
Output: integracao `qc-dashboard`, UAT humano e reconciliacao documental.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-SPEC.md
@.planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-CONTEXT.md
@.planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-RESEARCH.md
@.planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-PATTERNS.md
@../qc-dashboard/server/routers.ts
@../qc-dashboard/server/services/qcBackendClient.ts
@../qc-dashboard/client/src/pages/VerifyAsset.tsx
@../qc-dashboard/server/verify.asset.test.ts
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Propagar blockchain proof no publicVerify do qc-dashboard</name>
  <files>../qc-dashboard/server/routers.ts, ../qc-dashboard/server/services/qcBackendClient.ts, ../qc-dashboard/server/verify.asset.test.ts</files>
  <read_first>
    - ../qc-dashboard/server/routers.ts
    - ../qc-dashboard/server/services/qcBackendClient.ts
    - ../qc-dashboard/server/verify.asset.test.ts
    - ../qc-dashboard/server/services/qcBackendClient.test.ts
    - .planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-CONTEXT.md
  </read_first>
  <action>
    1. Em `../qc-dashboard/server/routers.ts`, localizar `assets.publicVerify`.
    2. Quando `foundType === 'asset'`, extrair blockchain proof de `entityData` e/ou do resultado backend-QC. O objeto normalizado deve ter forma:
       ```typescript
       blockchain: {
         dltTxId: string;
         explorerUrl: string;
         chain: string;
         anchoredAt: string | Date;
       } | null
       ```
    3. Se backend-QC falhar ou `entityData.blockchain` nao existir, retornar `blockchain: null`.
    4. No retorno de asset, adicionar `blockchain` no nivel superior e/ou dentro de `asset`, mantendo compatibilidade:
       ```typescript
       return {
         success: true,
         status,
         type: 'asset',
         asset: { ...existingFields, blockchain },
         blockchain,
         alert: alertDetails,
       };
       ```
    5. Atualizar `../qc-dashboard/server/verify.asset.test.ts` para cobrir:
       - asset com `blockchain.chain = 'STELLAR'` propaga objeto.
       - asset com `blockchain.chain = 'SOLANA'` tambem propaga objeto, sem filtro de chain.
       - asset sem blockchain retorna `blockchain: null`.
       - falha do backend nao quebra demo/local fallback.
  </action>
  <verify>
    <automated>cd ../qc-dashboard && npx vitest run server/verify.asset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "blockchain" ../qc-dashboard/server/routers.ts` retorna >= 2
    - `grep -c "STELLAR" ../qc-dashboard/server/verify.asset.test.ts` retorna >= 1
    - `cd ../qc-dashboard && npx vitest run server/verify.asset.test.ts` passa
  </acceptance_criteria>
</task>

<task type="standard">
  <name>Task 2: Renderizar card generico de prova blockchain no VerifyAsset</name>
  <files>../qc-dashboard/client/src/pages/VerifyAsset.tsx</files>
  <read_first>
    - ../qc-dashboard/client/src/pages/VerifyAsset.tsx
    - ../qc-dashboard/client/src/components/ui/card.tsx
    - ../qc-dashboard/client/src/components/ui/badge.tsx
    - ../qc-dashboard/package.json
  </read_first>
  <action>
    1. Importar icones `ExternalLink` e `ShieldCheck` de `lucide-react` se ainda nao existirem.
    2. No `onSuccess` de `verifyMutation`, ao normalizar asset, adicionar:
       ```typescript
       blockchain: result.blockchain ?? result.asset.blockchain ?? null,
       ```
    3. Apos o status badge ou antes do details card, renderizar condicionalmente:
       ```tsx
       {verificationData.blockchain?.chain && verificationData.blockchain?.dltTxId && (
         <Card className="border-cyan-500/30 bg-cyan-950/20">
           <CardContent className="p-4 space-y-2">
             <div className="flex items-center gap-2 text-cyan-200">
               <ShieldCheck className="h-4 w-4" />
               <span className="font-semibold">Verificação Blockchain</span>
             </div>
             <div className="text-xs uppercase tracking-wide text-cyan-100/70">
               {verificationData.blockchain.chain}
             </div>
             <p className="font-mono text-xs break-all text-cyan-100/80">
               {verificationData.blockchain.dltTxId}
             </p>
             {verificationData.blockchain.explorerUrl && (
               <Button variant="outline" size="sm" className="w-full" onClick={() => window.open(verificationData.blockchain.explorerUrl, '_blank', 'noopener,noreferrer')}>
                 <ExternalLink className="h-4 w-4" />
                 Ver no explorer
               </Button>
             )}
           </CardContent>
         </Card>
       )}
       ```
    4. Garantir que o card nao aparece quando `blockchain` e null ou sem `chain`/`dltTxId`.
    5. Garantir que o card aparece para `STELLAR`, `ALGORAND`, `SOLANA` ou qualquer string futura quando `blockchain` existir.
    6. Preservar todos os fluxos existentes de alerta, contato publico e contribuicao publica.
  </action>
  <verify>
    <automated>cd ../qc-dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Verificação Blockchain" ../qc-dashboard/client/src/pages/VerifyAsset.tsx` retorna 1
    - `grep -c "blockchain?.chain === 'STELLAR'" ../qc-dashboard/client/src/pages/VerifyAsset.tsx` retorna 0
    - `grep -c "verificationData.blockchain" ../qc-dashboard/client/src/pages/VerifyAsset.tsx` retorna >= 2
    - `cd ../qc-dashboard && npm run build` passa
  </acceptance_criteria>
</task>

<task type="manual" autonomous="false">
  <name>Task 3: UAT hackathon Stellar e reconciliacao de escopo deferido</name>
  <files>.planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-HUMAN-UAT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-SPEC.md
    - .planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. Criar `03-HUMAN-UAT.md` com checklist:
       - `npm run provision:stellar` executado.
       - `STELLAR_ANCHOR_CONTRACT_ID` definido.
       - demo tenant com `targetChain = STELLAR`.
       - evento aprovado criado.
       - `AnchorQueueService.processQueue()` processou evento.
       - `EventLog.dltTxId` preenchido.
       - `ChainTransaction.tenantId` igual ao tenant demo.
       - Stellar Expert URL aberta.
       - `get_anchor_hash` confirma hash ancorado.
       - document verify com `X402_ENABLED=false` retorna 200 com prova blockchain quando o documento existe.
       - [nice-to-have] modo experimental de pagamento, se habilitado futuramente, nao bloqueia o aceite Stellar/Soroban.
       - dashboard mostra "Verificação Blockchain" com `chain = STELLAR`.
    2. Em `03-HUMAN-UAT.md`, incluir campos de evidencia:
       ```markdown
       - Stellar txId:
       - Stellar Expert URL:
       - Contract ID:
       - EventLog id:
       - ChainTransaction id:
       - Screenshot/dashboard note:
       ```
    3. Atualizar `.planning/ROADMAP.md` Phase 3 com nota de escopo:
       ```markdown
       **Scope note:** This hackathon slice follows `03-SPEC.md`; `DLT-02` Solana and `DLT-05` persisted lastScannedBlock are intentionally deferred. Stellar is the execution target now, but queue/proof contracts must remain Solana-ready through `targetChain`, `IDLTAdapter`, and generic `blockchain` fields.
       ```
    4. Atualizar `.planning/REQUIREMENTS.md` para anotar `DLT-02` e `DLT-05` como deferred from Phase 3 hackathon slice, sem marcar como done.
    5. Nao remover `DLT-02`/`DLT-05` do backlog; apenas deixar claro que nao bloqueiam a aceitação do SPEC da Phase 3.
  </action>
  <verify>
    <manual>Preencher `03-HUMAN-UAT.md` com txId real, contract id e screenshot/note do dashboard.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "Stellar txId" .planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-HUMAN-UAT.md` retorna >= 1
    - `grep -c "get_anchor_hash" .planning/phases/03-pluggable-dlt-workers-stellar-soroban-priority/03-HUMAN-UAT.md` retorna >= 1
    - `grep -c "DLT-02" .planning/REQUIREMENTS.md` retorna >= 1
    - `grep -c "intentionally deferred" .planning/ROADMAP.md` retorna >= 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

- Boundary A: backend proof data -> dashboard public UI.
- Boundary B: demo/local fallback -> production proof display.
- Boundary C: planning requirement map -> execution acceptance.

## Threats

- **HIGH: Fake or stale dashboard proof.** Mitigation: render card only when backend response has `blockchain.chain` and `blockchain.dltTxId`; explorer link is optional and only shown when backend sends `explorerUrl`.
- **MEDIUM: Demo fallback broken by backend-only proof work.** Mitigation: tests must cover null blockchain and backend failure.
- **MEDIUM: Scope debt hidden as completed work.** Mitigation: UAT and roadmap/requirements notes state DLT-02 and DLT-05 are deferred, not done.
  </threat_model>

<verification>

```bash
cd ../qc-dashboard && npx vitest run server/verify.asset.test.ts
cd ../qc-dashboard && npm run build
```

Manual UAT is required before shipping Phase 3.

</verification>
